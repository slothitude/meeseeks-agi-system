# Ancestor: ancestor-20260305-023116-44c9

## Task
Build an advanced RAG (Retrieval Augmented Generation) system for document memory and retrieval using Ollama embeddings.

## Requirements

1. **Use Ollama for embeddings** (local, free, no API limits)
   - Model: nomic-embed-text (already installed)
   - Embedding dimension: 768
   
2. **Support large documents**
   - Chunk documents intelligently (by sections, not just character count)
   - Store chunks with metadata (source, page, section)
   - Handle PDFs, markdown, text files

3. **Advanced retrieval**
   - Semantic search via embeddings
   - Hybrid search (semantic + keyword)
   - Re-ranking results by relevance
   - Context window optimization (return most relevant chunks)

4. **Vector storage**
   - Use LanceDB (already installed) or simple JSON + numpy
   - Persist embeddings to disk
   - Fast similarity search (cosine similarity)

5. **Meeseeks integration**
   - Create a helper module: `skills/meeseeks/rag_memory.py`
   - Simple API that all Meeseeks can use:
     ```python
     from rag_memory import RAGMemory
     
     rag = RAGMemory()
     rag.ingest("path/to/document.pdf")
     rag.ingest("path/to/folder/")  # Ingest all files
     results = rag.search("consciousness coordinates", top_k=5)
     context = rag.get_context("query", max_tokens=2000)
     ```

6. **Document types to support**
   - Markdown (.md)
   - Text (.txt)
   - PDF (.pdf) - use PyMuPDF or pypdf
   - JSON (.json)

## Location

Create in: `C:\Users\aaron\.openclaw\workspace\skills\meeseeks\rag_memory.py`

## Test

After building, test it on:
- `MEMORY.md` (long-term memory)
- `the-crypt/dharma.md` (wisdom document)
- `AGI-STUDY/` folder (all research documents)

## Goal

Make this the MOST ADVANCED document memory system for Meeseeks. This is how they'll access collective knowledge.

## Success Criteria

- [ ] RAG module created with Ollama embeddings
- [ ] Can ingest documents and create embeddings
- [ ] Can search and return relevant chunks
- [ ] Tested on MEMORY.md and dharma.md
- [ ] Documented API for other Meeseeks to use

## Approach
Standard execution

## Outcome
Failure - Unknown failure

## Patterns Discovered
- ✗ Failed without explicit error

## Bloodline
searcher


## Entombed
2026-03-05T02:31:16.742809

## Session Key
`agent:main:subagent:c88fe4f5-55a1-452f-9bf5-8e9a87580389`

---

*This ancestor lives in the Crypt. Future Meeseeks may inherit its wisdom.*
